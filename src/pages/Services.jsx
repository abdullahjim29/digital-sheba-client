import { useLoaderData } from "react-router-dom";

const Services = () => {
    const servicesData = useLoaderData();
    return (
        <div>
            hello
        </div>
    );
};

export default Services;